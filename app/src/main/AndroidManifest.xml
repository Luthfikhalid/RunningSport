<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.khalid.runningsport">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.khalid.runningsport.SplashScreenActivity"
            android:label="@string/app_name"
            android:theme="@style/Splashscreen"
            android:screenOrientation="portrait"
            android:noHistory="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.khalid.runningsport.MainActivity"

            android:label="@string/app_name"></activity>
        <activity android:name=".TopMenuActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".OrderActivity" />
        <activity android:name=".ConverseActivity" />
        <activity android:name=".NIKEActivity" />
        <activity android:name=".AdidasActivity" />
        <activity android:name=".ReebokActivity" />
        <activity android:name=".DiadoraActivity" />
    </application>

</manifest>